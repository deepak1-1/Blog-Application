const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const blogSchema = new Schema({
    title: {
        type: String,
        required: true
    },
    about: {
        type: String,
        required: true
    },
    data: {
        type: String,
        required: true
    }
    <!-- gmail: {
        type: string,
        required: true
    } -->
}, {timestamps : true});

const blodModel = mongoose.model('LoginData', logSchema);
module.exports = blogModel;